# Supabase Postgres — get from Supabase Dashboard → Project Settings → Database → Connection String
# Use pooled connection (port 6543) for Vercel/serverless, direct (5432) for local migrate
DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres?pgbouncer=true"
# For local prisma migrate with direct connection, uncomment:
# DIRECT_URL="postgresql://postgres.[project-ref]:[password]@aws-0-ap-southeast-1.pooler.supabase.com:5432/postgres"

NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="ganti-dengan-random-32-char-min-untuk-production"

# Supabase Realtime - untuk fitur realtime dashboard/POS
# Dapatkan dari Supabase Dashboard -> Project Settings -> API -> Project URL & anon public key
NEXT_PUBLIC_SUPABASE_URL="https://dpgagtknkcbrbeblitqf.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="isi-dengan-anon-key-dari-dashboard"
# SUPABASE_SERVICE_ROLE_KEY="..." (hanya server, jangan expose)

# Jika anon key belum diisi, fallback polling otomatis tiap 8-15 detik tetap aktif (tetap terasa realtime)
